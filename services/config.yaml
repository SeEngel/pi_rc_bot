# services/config.yaml
#
# Controls which set of services `services/main.sh` starts.
#
# - legacy:           monolithic services + legacy advisor agent
# - split_brain_move: brain services + move cluster + split-brain advisor agent
# - open_code:        brain services + move cluster + OpenCode supervisor (recommended)

workflow_mode: open_code
