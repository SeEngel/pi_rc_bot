## Head service configuration

head:
  dry_run: false
  # Base URL of the shared hardware owner service.
  robot_base_url: http://127.0.0.1:8010
  max_pan_deg: 35
  max_tilt_deg: 35

  # Default scan behavior
  scan:
    step_deg: 5
    interval_s: 0.08

service:
  host: 0.0.0.0
  port: 8006
