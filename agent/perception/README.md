# Perception agent

Agent Framework-based agent for lightweight visual detections via `services/perception` MCP.

## Run

- MCP smoke test:
  - `uv run python agent/perception/main.py --mcp-smoke-test`
- Direct detect (no LLM):
  - `uv run python agent/perception/main.py --direct`
